#!/usr/bin/env bash

if ! command -v foreman &> /dev/null
then
  echo "Installing foreman..."
  gem install foreman
fi

echo -e "web: rails server -p 3000\ncss: yarn build:css --watch\njs: yarn build --watch" > /tmp/Procfile.rails_buddy_dev

foreman start -f /tmp/Procfile.rails_buddy_dev --root=.
